# Declarative description of a gasa end-to-end test fixture repository.
# Managed by `make fixtures-apply`; do not edit the repository directly.
repo: bretfisher/gasa-pass
private: false
actions:
    enabled: true
    allowed_actions: selected
    default_workflow_permissions: read
    can_approve_pull_request_reviews: false
    fork_pr_contributor_approval: all_external_contributors
purpose: Known-good repository. Every rule must pass here, which also makes it a reference an operator can copy from.
unmanaged:
    - .github/workflows/daily-repo-status.lock.yml
    - .github/workflows/daily-repo-status.md
